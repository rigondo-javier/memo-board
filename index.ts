// Import stylesheets
import { Sayer } from './Sayer';
import './style.css';

// Write TypeScript code!
const appDiv: HTMLElement = document.getElementById('app');
appDiv.innerHTML = `<h1>TypeScript Starter</h1>`;
/*
const a = new Sayer();
(async () => {
  for (let i = 0; i < 11; ++i) {
    console.log('before ' + i);
    await a.say(String(i));
    console.log('after ' + i);
  }
})();
*/
